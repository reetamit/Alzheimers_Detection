services:
  - type: web
    name: alzheimer-api
    env: python
    repo: https://github.com/reetamit/Alzheimers_Detection
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    autoDeploy: true
